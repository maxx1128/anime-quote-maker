import React from "react";
import QuoteWrapper from "./components/QuoteWrapper";

class Random extends React.Component {
  render() {
    return (
      <QuoteWrapper view={'noUI'}/>
    );
  }
}

export default Random;
